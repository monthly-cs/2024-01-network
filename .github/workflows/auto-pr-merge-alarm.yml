name: PR 병합 알람

on:
  pull_request:
    types: ["closed"]

jobs:
  create-pr:
    runs-on: ubuntu-20.04

    steps:
      - name: Check out the code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          
      - name: Auto Alarm
        env:
          GH_TOKEN: ${{ github.token }}

        run: |
          echo "hello"
          
          mail -s "GitHub Action 성공" workstation19961002@gmail.com
